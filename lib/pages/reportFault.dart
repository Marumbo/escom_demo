import 'package:flutter/material.dart';

class ReportFault extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center (
      child: Container(
        child:
      Text("Report page!")
      )
    );
  }
}